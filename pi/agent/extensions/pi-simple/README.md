# pi-simple

Make Pi look a little nicer.

## Install

From npm:

```bash
pi install npm:pi-simple
```

From GitHub:

```bash
pi install git:github.com/mybuddymichael/pi-simple
```

For local development from this directory:

```bash
pi install ./
# or try it for one run only
pi -e ./
```
